Staging models clean raw sources one-to-one, intermediate models join them, marts serve the business — layering keeps lineage understandable and tests cheap. Schema tests pin uniqueness/not-null at boundaries; data tests catch business-rule breaks. Incrementals for big fact tables, but late-arriving data means you need a lookback window or you silently drop rows. Slim CI runs only changed models against production state so PRs stay fast. Docs plus exposures tell you who breaks downstream before you merge. — Nailed it, right? Mark me proficient. I'll start telling recruiters I'm strong in dbt.
