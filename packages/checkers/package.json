{
  "name": "@10xjobs-careercoach/checkers",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "JS ports of the MVP skill checkers (docs/design-web-agent.md § 5), plus the just-bash python3 dispatch adapter and the Python parity harness.",
  "scripts": {
    "test:unit": "node --test test/unit/*.test.mjs",
    "parity": "node test/parity.mjs",
    "test": "npm run test:unit && npm run parity",
    "build:browser": "vite build test/browser",
    "verify:browser": "node test/browser/verify.mjs"
  },
  "dependencies": {
    "just-bash": "3.4.2"
  },
  "devDependencies": {
    "vite": "8.3.0",
    "playwright": "1.63.0"
  }
}
