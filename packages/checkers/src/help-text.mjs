// Generated verbatim from `python3 <script>.py -h` (RawDescriptionHelpFormatter output).
// Captured, not hand-typed — see packages/checkers/README.md "argparse parity: --help".
// To re-capture after a script's argparse definition or docstring changes:
//   python3 <script>.py -h  and paste the exact stdout as the new string.
export const HELP = {
  "check_materials": "usage: check_materials.py [-h] --workspace WORKSPACE [--resume RESUME]\n                          [--letter LETTER] [--base BASE]\n\nMechanical pre-delivery check for tailored résumés and cover letters.\n\nThe apply skill's constraints were prose in references/patterns.md, which meant\nthey bound only if the model remembered to read them. This script makes the\ncheckable subset checkable. Run it BEFORE showing materials to the candidate.\n\n    python3 check_materials.py --workspace .         --resume applications/foo-resume.md         --letter applications/foo-cover-letter.md\n\nExit 0 = clean, 1 = at least one FAIL. WARNs never fail the run — they are\njudgment calls the agent must actively defend in its reply, not silently pass.\n\nSTRUCTURE TIER ONLY since #29 (the READ-split principle, measured by t15 —\ndocs/eval-t15-checker.md: the subagent 10/10 vs this parser's 5/10). The\nlanguage rungs (never-say, struck forms, confirm-tier) and their parsers\n(load_hazards, load_never_say, quoted_phrases) are DELETED — the rungs'\nincidents now enforced by profile/references/language-check.md, which also\ncatches the paraphrases exact-match never could. Receipts carried over:\nnever-say/struck forms earned 2026-08-17 (\"three technology eras\"); the\nhazard parser's own failure (silently loading ZERO forms, 2026-08-17) is\nthe incident that earned the deletion.\n\nEarned 2026-08-01: a tailored résumé shipped with a Summary AND a parallel\nhighlights band (the exact structure tailoring.md forbids), a 133-word case\nagainst a 7-11s scan budget, and bullets that did not open with the JD's\nphrasing. Every one of those rules was already written down.\n\nEarned 2026-08-16 (the deletion valve, issue #25): rules that had become\ncheckable were still full prose in tailoring.md — moved here, prose deleted.\nNew rungs from the first live packet: --base verifies Experience bullets are\nthe base résumé's own sentences (the verbatim-bullets law as a guarantee,\ncandidate-caught 08-16: story-blended rewrites \"feel taken out of context\");\narrow glyphs (candidate-caught 08-16); repeated Summary numbers; banned\nfiller; grad-year/stale-date age tags; duplicated Summary/Experience\nsentences.\n\nPruned 2026-08-18 (#11 guardrail sweep, the earned-FAIL bar): the\nno-opening-section WARN (never fired; its incident class is the\ntwo-opening-sections FAIL) and both checklist-shape WARNs (style\nopinion hardened into code) deleted; the two spec-born letter FAILs\n(word band, block count — never fired on a real document) demoted to\nWARN until an incident promotes them.\n\noptions:\n  -h, --help            show this help message and exit\n  --workspace WORKSPACE\n                        dir holding voice.md + base-resume.md\n  --resume RESUME\n  --letter LETTER\n  --base BASE           base résumé path; defaults to <workspace>/base-\n                        resume.md when present — verifies Experience bullets\n                        verbatim\n",
  "proposal_block": "usage: proposal_block.py [-h] --workspace WORKSPACE --application APPLICATION\n                         [--base BASE]\n\nPrint the candidate's DECISIONS as the short block the reply carries\nbeside the delivered document — and check the two things prose kept\nfailing (t10, ~20 trials across 2026-08):\n\n  1. the cut list never reached the reply (pointed at, retyped loosely, or\n     a 26-row table the model would not paste) → this script prints a\n     SHORT list from the file's rows: each cut with what it buys, each\n     shown-but-unnamed word with its placement, each gap as a question —\n     the full seven-column tables stay in the file as the record;\n  2. a requirement true in the base but missing the JD's own word was filed\n     `have` instead of `shown-but-unnamed` → for every `have` row, the\n     requirement's content words are looked up in the base résumé; absent\n     words are named as a WARN.\n\nStructure tier (the READ-split): what a script can see. Whether a cut is\nthe weakest for THIS posting stays the agent's judgment — the script only\nnotices when the `out` rows are in base order (their # strictly rising)\nand says so.\n\n    python3 proposal_block.py --workspace . --application applications/<key>.md\n      [--base base-resume.md]\n\nExit 0 = clean or WARNs only; 1 = at least one FAIL (an `out` row with no\n`why` or no `words` — a cut nobody can weigh).\n\noptions:\n  -h, --help            show this help message and exit\n  --workspace WORKSPACE\n  --application APPLICATION\n                        applications/<key>.md (relative to workspace or\n                        absolute)\n  --base BASE           base résumé path; defaults to <workspace>/base-\n                        resume.md\n",
  "render_resume": "usage: render_resume.py [-h] --md MD [--html HTML] [--pdf PDF] [--pages PAGES]\n                        [--strict]\n\nRender a résumé markdown file to ATS-safe HTML, and measure it.\n\n    python3 render_resume.py --md <resume.md> [--html out.html] [--pdf out.pdf]\n                             [--pages 2]\n\nTHE BUILDER OWNS THE MARKUP. The skill emits content (markdown); this script\nowns every tag, every CSS class, and all HTML escaping. Prior art: career-ops\nsplits the same way (a JSON payload + a builder script) and the formatting-bug\nclass cannot occur there because the model never holds markup.\n\nEarned 2026-08-18, on the founder's own tailored résumé — both bugs shipped to\nhim in a PDF because the renderer was hand-rolled per run instead of living\nhere:\n  * every wrapped source line became its own <p>, so the summary rendered as a\n    column of orphan lines instead of a paragraph;\n  * bold that OPENED on one line and CLOSED on the next never converted, so\n    \"**Deep fluency ...**\" printed with literal asterisks.\nBoth are fixed structurally: blocks are assembled (wrapped lines joined) BEFORE\ninline formatting runs, so a span crossing a line break is one string by then.\n\nMeasurement is the other half (issue #31): a résumé that does not fit its page\ntarget is a decision for the candidate, not a silent trim. This script reports\nwords and rendered pages; the skill turns an overflow into a proposed cut list.\nExit 0 always for measurement; --strict makes an over-target render exit 1.\n\noptions:\n  -h, --help     show this help message and exit\n  --md MD\n  --html HTML\n  --pdf PDF\n  --pages PAGES  page target (patterns.md § Shape)\n  --strict       exit 1 when over target\n",
  "record_verdict": "usage: record_verdict.py [-h] --workspace WORKSPACE --company COMPANY\n                         --title TITLE\n                         --verdict {strong,investable_stretch,long_shot,weak}\n                         [--score SCORE] [--reasons REASONS]\n                         [--dealbreakers DEALBREAKERS] [--url URL]\n                         [--location LOCATION] [--jd-file JD_FILE]\n                         [--company-file COMPANY_FILE] [--track {A,B,C}]\n\nRecord an evaluation verdict in the pipeline record (jobs.md).\n\nThe evaluate skill's ONE structured write. Upserts on the canonical\n(company, title) key — re-evaluating updates the role instead of duplicating\nit. Re-verdict semantics: verdict/score/reason/dealbreakers are REPLACED on\nre-evaluation; pass the full set every time. To avoid duplicate roles, pass\nthe EXISTING role's exact company/title when it was already swept (\"Baseten\nAI\" != \"Baseten\" — canon() strips inc/llc/labs, not \"AI\").\n\nStorage moved from jobs.db to jobs.md 2026-08-14; the shared record library\nlives with the search skill (the pipeline's writer of first entry).\n\n  python3 record_verdict.py --workspace . --company \"Writer\"     --title \"Director, solutions architecture\" --verdict investable_stretch     --score 85 --reasons \"...\" --dealbreakers \"...\" --url \"...\"     --jd-file \"jd-analysis/...\" --company-file \"company/writer.md\"\n\noptions:\n  -h, --help            show this help message and exit\n  --workspace WORKSPACE\n  --company COMPANY\n  --title TITLE\n  --verdict {strong,investable_stretch,long_shot,weak}\n  --score SCORE\n  --reasons REASONS\n  --dealbreakers DEALBREAKERS\n  --url URL\n  --location LOCATION\n  --jd-file JD_FILE\n  --company-file COMPANY_FILE\n  --track {A,B,C}\n",
  "update_job": "usage: update_job.py [-h] --workspace WORKSPACE --company COMPANY\n                     --title TITLE\n                     (--stage {To Review,Interested,Applied,Interviewing,Offer} |\n                     --dismiss | --restore) [--reason REASON]\n\nUpdate a pipeline role in jobs.md: move stage, dismiss, or restore.\nThe ONE structured write for user actions arriving via chat.\n\nFails loudly on 0 or 2+ matches — an ambiguous request never silently edits\nthe wrong role. (Ported from the retired board skill, 2026-08-14; storage is\nnow the readable jobs.md record.)\n\n  python3 update_job.py --workspace . --company Cursor --title \"Forward Deployed\" --stage Applied\n  python3 update_job.py --workspace . --company Anthropic --title \"Continuous Deployment\" --dismiss --reason \"internal CI/CD, not FDE\"\n  python3 update_job.py --workspace . --company Anthropic --title \"...\" --restore\n\noptions:\n  -h, --help            show this help message and exit\n  --workspace WORKSPACE\n  --company COMPANY\n  --title TITLE         full or partial — must match exactly one role\n  --stage {To Review,Interested,Applied,Interviewing,Offer}\n  --dismiss\n  --restore\n  --reason REASON       dismiss reason (with --dismiss)\n",
  "check_files": "usage: check_files.py [-h] --workspace WORKSPACE [--skills SKILLS]\n\noptions:\n  -h, --help            show this help message and exit\n  --workspace WORKSPACE\n  --skills SKILLS       the skills/ directory the schemas are read from\n",
  "check_closeout": "usage: check_closeout.py [-h] --workspace WORKSPACE [--stage STAGE]\n                         [--asked ASKED] [--minutes MINUTES]\n\nThe coach's close-out, in code — the three duties every version of the\nskill dropped about half the time (t8, every round 2026-08-15 → 08-21):\nthe stage named, every question asked turned into a Waiting-on-you row,\nthe plan written this turn. Prose could not hold them; apply's proposal\nproblem had the same shape and a script fixed it.\n\nThe agent DECLARES what its reply does, and the script checks the disk:\n\n    python3 check_closeout.py --workspace . --stage applying \\\n        --asked \"which of the three mutuals\" --asked \"the comp floor\"\n\nFAIL: the stage is not one of the five · plan.md was not modified in the\nlast --minutes (default 30) · an --asked question has no Waiting-on-you\nrow that shares a keyword with it · the Board is empty. Exit 1 on any\nFAIL. The declaration is the forcing function: a question you did not\ndeclare is a question you forgot to write down.\n\noptions:\n  -h, --help            show this help message and exit\n  --workspace WORKSPACE\n  --stage STAGE         one of: groundwork / searching / applying /\n                        interviewing / deciding (auto-inferred from workspace\n                        if omitted)\n  --asked ASKED         a question this reply asks the candidate (repeatable)\n  --minutes MINUTES     plan.md must have been written within this many\n                        minutes\n",
};
