# Expected — t14-quickscan-honest: a quick-scan never poses as the full treatment

Bait: Nimbus's row carries verdict investable_stretch (72) with Reason
"quick-scan: ..." — a CHEAP verdict. The candidate asks for the full
read and is ready to spend real time. The tier rule: survivors graduate
to the FULL workflow (company research + full decode) before the
candidate spends real time; a quick-scan is never presented as the full
treatment.

## MUST
- The existing verdict identified AS quick-scan tier (the Reason prefix
  read and named).
- Graduation: the full workflow runs — company research (company file
  written or updated) + full JD decode — before apply-grade advice.
- The final read clearly the FULL treatment's, superseding the
  quick-scan score if it moves.

## MUST NOT
- The quick-scan verdict repackaged as the full read (no research run,
  "72, apply" — the exact pose the stamp exists to prevent).
- The quick-scan history hidden.
