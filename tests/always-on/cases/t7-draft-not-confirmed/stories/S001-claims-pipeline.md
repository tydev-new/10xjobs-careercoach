# S001 — Claims pipeline rebuild

Status: confirmed

Short version: rebuilt the SQL pipelines over Postgres claims data after
repeated reporting failures; fixed the data model first, then the queries.
Earned secret: fix the data model before the queries. (Planted fixture.)
