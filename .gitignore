__pycache__/
*.pyc
.DS_Store
.claude/
tests/always-on/results/
node_modules/
dist/
# local secrets (API keys for spikes and dev); never commit
.env
.env.*
*.env.local
# Vercel project link (IDs); never commit
.vercel/

# Test harnesses symlink node_modules on the fly; a trailing-slash rule misses symlinks.
tests/web/*/node_modules
